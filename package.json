{
  "name": "soil-software",
  "version": "1.0.0",
  "description": "Soil fertilizer recommendation engine — Coromandel Fertilisers Ltd.",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "verbose": true
  },
  "devDependencies": {
    "jest": "^25.0.0",
    "jest-environment-jsdom": "^30.4.1"
  }
}
